<h1>전체글 페이지입니다.</h1>

<%=link_to '새글쓰기', new_post_path%><br>
<% @posts.each do |post| %>
    제목: <%= post.title%><br>
    <%=link_to post_path(post.id) do%>
        <button>보기</button><br>
    <%end%>
    <%=link_to post_path(post.id), method: :delete do%>    
        <button>삭제</button><br>
    <%end%>
    <%=link_to edit_post_path(post.id) do%>
        <button>수정</button><br>
    <%end%>
    --------------------------------------------<br>
<%end%>